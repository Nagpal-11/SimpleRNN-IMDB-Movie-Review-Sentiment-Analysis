# SimpleRNN-IMDB-Movie-Review-Sentiment-Analysis
An IMDB Sentiment Analysis project using Simple RNN classifies movie reviews as positive or negative. It tokenizes and pads text, maps words using an Embedding layer, and uses a SimpleRNN layer to capture sequential context. The final Dense layer with Sigmoid activation outputs the binary prediction.
